# Finite State Morphism

A compact Haskell implementation for mapping numeric observations into discrete state categories.

The model applies a deterministic classification function across a bounded observation sequence and produces paired observation-state outputs.

## Structure

finite-state-morphism/
├── README.md
└── Main.hs

## Implementation

`Main.hs` contains the state classifier, observation sequence, functional mapping, and formatted output pipeline.
